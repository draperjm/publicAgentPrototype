FROM python:3.9-slim

WORKDIR /app

COPY requirements_variance_validator.txt .
RUN pip install --no-cache-dir -r requirements_variance_validator.txt

COPY variance_validator.py .

EXPOSE 8093

CMD ["uvicorn", "variance_validator:app", "--host", "0.0.0.0", "--port", "8093"]
