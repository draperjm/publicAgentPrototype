"""
Common Agent Framework
======================
Shared utilities for all specialist agents in the pipeline.

Usage in an agent module:
    from common.agent import create_app
    from common.llm import LLMClient
    from common.output import OutputManager
    from common.config import settings
    from common.models import AgentError
"""
